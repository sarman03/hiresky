# Copy to .env and fill in. .env is git-ignored.
GEMINI_API_KEY=<YOUR_GEMINI_API_KEY>

# Optional: override the WebSocket bind (otherwise taken from config.yaml)
# HIRESKY_WS_HOST=127.0.0.1
# HIRESKY_WS_PORT=8765
